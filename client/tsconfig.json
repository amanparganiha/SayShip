{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "types": ["vite/client"]
  },
  // server/runtime holds browser code (bundled into generated apps), so it is checked with DOM types.
  "include": ["src", "../shared", "../server/runtime"]
}
